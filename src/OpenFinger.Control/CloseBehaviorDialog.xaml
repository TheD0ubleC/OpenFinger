<Window x:Class="OpenFinger.Control.CloseBehaviorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="关闭 OpenFinger"
        Width="560"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource BrushBg}"
        FontFamily="Microsoft YaHei UI, Segoe UI, sans-serif"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">
    <Window.Resources>
        <Style x:Key="ChoiceCardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource BrushPanel}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,15" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BrushActiveBorder}" />
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource BrushHover}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BrushActiveBorder}" />
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource BrushControlPressed}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChoiceCardPrimaryStyle" TargetType="Button" BasedOn="{StaticResource ChoiceCardButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource BrushPrimarySoft}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BrushActiveBorder}" />
        </Style>

        <Style x:Key="DialogTextButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource BrushSubText}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource BrushText}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.72" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="18"
            Padding="24"
            Style="{StaticResource CardBorderStyle}">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Width="48"
                        Height="48"
                        Background="{DynamicResource BrushPrimarySoft}"
                        BorderBrush="{DynamicResource BrushActiveBorder}"
                        BorderThickness="1"
                        CornerRadius="14">
                    <Path Width="22"
                          Height="22"
                          Stretch="Uniform"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Fill="{DynamicResource BrushPrimary}"
                          Data="M12,2A10,10 0 1 1 2,12H5.5A6.5,6.5 0 1 0 12,5.5V2M11,7H13V13H8V11H11V7Z" />
                </Border>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="关闭窗口时怎么处理"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{DynamicResource BrushText}" />
                    <TextBlock Margin="0,8,0,0"
                               Text="你可以直接退出，也可以让窗口进入托盘继续运行。这个选择以后还能在“设置”里改。"
                               Foreground="{DynamicResource BrushSubText}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Grid>

            <Grid Margin="0,22,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Style="{StaticResource ChoiceCardButtonStyle}"
                        Click="OnCloseClick">
                    <StackPanel>
                        <TextBlock Text="直接关闭"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource BrushText}" />
                        <TextBlock Margin="0,6,0,0"
                                   Text="退出 OpenFinger，停止当前客户端窗口和本地前台会话。"
                                   Foreground="{DynamicResource BrushSubText}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Button>

                <Button Grid.Column="2"
                        Style="{StaticResource ChoiceCardPrimaryStyle}"
                        Click="OnTrayClick">
                    <StackPanel>
                        <DockPanel LastChildFill="False">
                            <Border DockPanel.Dock="Right"
                                    Background="{DynamicResource BrushPrimarySoft}"
                                    BorderBrush="{DynamicResource BrushActiveBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    MinWidth="42"
                                    Padding="8,2">
                                <TextBlock Text="推荐"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource BrushPrimary}" />
                            </Border>
                            <TextBlock Text="最小化到托盘"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource BrushText}" />
                        </DockPanel>
                        <TextBlock Margin="0,6,0,0"
                                   Text="点关闭后窗口消失，但设备连接、服务和状态仍继续运行。"
                                   Foreground="{DynamicResource BrushSubText}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Button>
            </Grid>

            <CheckBox x:Name="RememberChoiceCheckBox"
                      Margin="0,16,0,0"
                      Style="{StaticResource AppCheckBoxStyle}"
                      Content="以后不再提示，记住这次选择" />

            <Border Margin="0,18,0,0"
                    Background="{DynamicResource BrushInputReadonly}"
                    BorderBrush="{DynamicResource BrushBorder}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="14,12">
                <TextBlock Text="如果之后想重新弹出这个窗口，可以到“设置 → 窗口与托盘”里重新启用关闭提示。"
                           Foreground="{DynamicResource BrushSubText}"
                           TextWrapping="Wrap" />
            </Border>

            <DockPanel Margin="0,18,0,0" LastChildFill="False">
                <Button DockPanel.Dock="Right"
                        Style="{StaticResource DialogTextButtonStyle}"
                        Content="取消"
                        Click="OnCancelClick" />
            </DockPanel>
        </StackPanel>
    </Border>
</Window>
