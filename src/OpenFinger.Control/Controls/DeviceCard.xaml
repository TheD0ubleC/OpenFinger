<UserControl x:Class="OpenFinger.Control.Controls.DeviceCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Style="{StaticResource CardBorderStyle}">
        <StackPanel>
            <TextBlock Text="{Binding DeviceName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource BrushText}"
                       Margin="0,0,0,12" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <Ellipse Width="8" Height="8" Fill="{Binding StatusBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="0,0,8,0" VerticalAlignment="Center" />
                <TextBlock Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="{DynamicResource BrushSubText}"
                           FontSize="13"
                           VerticalAlignment="Center" />
            </StackPanel>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="佩戴方式" VerticalAlignment="Center" Foreground="{DynamicResource BrushSubText}" FontSize="13" />
                <ComboBox Grid.Column="1" SelectedIndex="0" Style="{StaticResource SelectInputStyle}">
                    <ComboBoxItem Content="左手" />
                    <ComboBoxItem Content="右手" />
                </ComboBox>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <Button Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" Content="配网" />
                <Button Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Content="识别" />
                <Button Style="{StaticResource SecondaryButtonStyle}" Content="删除" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
